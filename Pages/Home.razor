@page "/"

<PageTitle>Home</PageTitle>

<Button Value="Add Tab" OnClick="@add"/>

<hr/>

<DynamicTab />


@code {

    [Inject] public TabManager tabManager { get; set; } = default!;

    protected override void OnInitialized()
    {
        tabManager.AddTab(nameof(TestComponent1),"first tab",true);
    }

    private void add()
    {
        tabManager.AddTab(nameof(TestComponent2));
        MessageBox.Show("Tab Added Successfully");
    }
}